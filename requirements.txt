# Union of both source repos' runtime deps + the gateway's own.
#
# pii_pipeline (SENTINEL-2.0) -- installed editable from ./SENTINEL-2.0
cryptography>=41
pynacl>=1.5
redis>=5
# Real ONNX NER model -- monoai_gateway.pii.PiiGuard opts into this by
# default (PII_USE_ONNX_NER=true) for much better name/address recall than
# the rule-based fallback (e.g. it catches lowercase names). Falls back to
# rule-based automatically if these aren't installed.
onnxruntime
tokenizers

# monoai_router (Lite_Multimodel_switching) -- installed editable from
# ./Lite_Multimodel_switching
pydantic>=2.0
groq>=0.9
httpx>=0.27

# gateway
fastapi>=0.110
uvicorn[standard]>=0.29

# tests
pytest>=8.0
pytest-asyncio>=0.23

# Install the two source packages themselves with:
#   pip install -e ./SENTINEL-2.0 -e ./Lite_Multimodel_switching -r requirements.txt
#
# To run without onnxruntime/tokenizers (rule-based NER fallback only,
# lower recall -- e.g. misses uncapitalized names), set PII_USE_ONNX_NER=false.
